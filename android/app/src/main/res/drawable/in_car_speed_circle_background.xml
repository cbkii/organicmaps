<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
  <solid android:color="@color/bg_cards" />
  <!-- Opaque black keyline is intentional: it survives high-glare light-map backgrounds better
       than the translucent locator outline used by less critical map controls. -->
  <stroke
      android:width="2dp"
      android:color="#FF000000" />
</shape>
